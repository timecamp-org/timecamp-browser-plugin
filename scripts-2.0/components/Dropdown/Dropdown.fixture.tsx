import * as React from 'react';
import Dropdown from '.';


export default <Dropdown isDisabled={false} text={'test 2'} onBackdropClick={() => {}} onDropdownButtonClick={() => {}} isOpen={true}>{'TEST'}</Dropdown>;
